# install.packages("forecast")
require("zoo")
require("forecast")

args = commandArgs(trailingOnly=TRUE)
dat <- read.csv(args[1])

# build model
dat <- read.zoo(dat)
m <- auto.arima(dat)

pdf(paste(m,".pdf",sep=""))
plot(forecast(m,h=100))
dev.off()

print(summary(m))
